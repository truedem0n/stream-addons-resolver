info:
  name: Clear Cache
  type: http
  seq: 11

http:
  method: delete
  url: "{{baseUrl}}/api/cache?type=all"
  params:
    - name: type
      value: all
      type: query
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
