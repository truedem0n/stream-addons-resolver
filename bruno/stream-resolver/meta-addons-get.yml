info:
  name: Get Meta Addons
  type: http
  seq: 16

http:
  method: get
  url: "{{baseUrl}}/api/meta-addons"
  auth: inherit

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
